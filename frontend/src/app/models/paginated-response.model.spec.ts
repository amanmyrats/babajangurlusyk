import { PaginatedResponse } from './paginated-response.model';

describe('PaginatedResponse', () => {
  it('should create an instance', () => {
    expect(new PaginatedResponse()).toBeTruthy();
  });
});
